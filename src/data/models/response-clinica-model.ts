export type ResponseClinicaModel = {
  data: {
    clinica: {
      id: string;
      nome: string;
      telefone: string;
      endereco: string;
      tipo: string;
      latitude: string;
      logintude: string;
    };
  };
};
